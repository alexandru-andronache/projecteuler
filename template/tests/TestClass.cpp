#include "TestClass.h"
#include "../test.h"

namespace problem_XXX {
    TEST_F(Test_problem_XXX, solve_test) {
        ASSERT_EQ(solve(0), 0);
    }

    TEST_F(Test_problem_XXX, solve_real_test) {
        ASSERT_EQ(solve(0), 0);
    }
}